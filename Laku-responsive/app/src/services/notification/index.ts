// Service notifikasi (scaffolding). Saat ini notifikasi di-generate lokal di
// AppContext. Saat backend siap, pindahkan GET/PATCH /notifications ke sini
// (lihat docs/API.md §8).
export const notificationService = {
  // list: () => apiClient<{ data: Notification[] }>(ENDPOINTS...),
  // markRead: (id: string) => apiClient(...),
};
